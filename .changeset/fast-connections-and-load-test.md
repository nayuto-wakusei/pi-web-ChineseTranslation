---
"@chainingintention/pi-web-cn": patch
---

Improve sessiond connection reuse and disconnect slow realtime clients before their outbound buffers grow without bound. Add a dependency-free local load runner for measuring HTTP, WebSocket, event-loop, CPU, and memory behavior under concurrent clients.
