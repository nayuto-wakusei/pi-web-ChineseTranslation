---
"@chainingintention/pi-web-cn": patch
---

Restore the chat composer when a selected session is reopened in the stacked workspace layout, so the prompt box no longer disappears after returning from workspace tools.
