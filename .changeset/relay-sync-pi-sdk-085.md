---
"@chainingintention/pi-web-cn": patch
---

Force-sync management-mode project Relay skills to the current bundled template and avoid duplicate warnings for identical project and bundled copies, without modifying project skills in normal mode.

Support Pi SDK 0.85.0 with its required server dependency, constrain SDK compatibility to the tested minor series, and verify web and session daemon startup after package installation.
